import { PipeStateTaskPipe } from './pipe-state-task.pipe';

describe('PipeStateTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeStateTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
